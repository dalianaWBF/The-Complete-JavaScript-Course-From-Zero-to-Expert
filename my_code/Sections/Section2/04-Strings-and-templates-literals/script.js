const firstName = 'Daliana';
const birthdate = 1996;
const year = 2023;

//Template literals use `` and you can insert any JS inside of it
const myName = `I'm ${firstName} and I'm ${year - birthdate} years old`;
console.log(myName);