//Expressions produce values:
    3 + 4
    '23 is bigger'

// Statements are full sentences that translate action:
// (if something end in a semicolon is a Statements)

    if (23 > 10) {
        const str = '23 is bigger';
    }